---
import BaseLayout from "../../layouts/BaseLayout.astro";
const base = import.meta.env.BASE_URL;
---

<BaseLayout title="Restauration d’une église du XVᵉ siècle — Barthès Maçonnerie">
  <article>
    <h2>Restauration d’une église du XVᵉ siècle</h2>

    <p>
      Enduits lissés à la chaux traditionnelle, restauration des arcs brisés
      et patinage pour une intégration fidèle à l’existant.
    </p>

    <p>
      Un travail mené dans le respect du bâti ancien et du savoir-faire traditionnel,
      où chaque intervention vise à préserver l’authenticité de l’ouvrage
      et la cohérence architecturale de l’ensemble.
    </p>

    <!-- Carousel -->
    <div class="carousel">
      <div class="carousel-track">
        <img
          src={`${base}images/realisations/eglise-xve/01.jpg`}
          alt="Église du XVᵉ siècle – vue intérieure"
          loading="lazy"
        />
        <img
          src={`${base}images/realisations/eglise-xve/02.jpg`}
          alt="Restauration des arcs brisés"
          loading="lazy"
        />
        <img
          src={`${base}images/realisations/eglise-xve/03.jpg`}
          alt="Enduit traditionnel à la chaux lissée"
          loading="lazy"
        />
        <img
          src={`${base}images/realisations/eglise-xve/04.jpg`}
          alt="Patinage et intégration à l’existant"
          loading="lazy"
        />
      </div>

      <button class="carousel-btn prev" aria-label="Image précédente">‹</button>
      <button class="carousel-btn next" aria-label="Image suivante">›</button>
    </div>
  </article>

 <script>
  document.addEventListener("DOMContentLoaded", () => {
    const carousel = document.querySelector(".carousel");
    if (!carousel) return;

    const track = carousel.querySelector(".carousel-track");
    const slides = Array.from(track.children);
    const prevBtn = carousel.querySelector(".carousel-btn.prev");
    const nextBtn = carousel.querySelector(".carousel-btn.next");

    let index = 0;

    const goToSlide = (i) => {
      index = Math.max(0, Math.min(i, slides.length - 1));
      slides[index].scrollIntoView({
        behavior: "smooth",
        inline: "center"
      });
    };

    prevBtn?.addEventListener("click", () => {
      goToSlide(index - 1);
    });

    nextBtn?.addEventListener("click", () => {
      goToSlide(index + 1);
    });
  });
</script>

</BaseLayout>
