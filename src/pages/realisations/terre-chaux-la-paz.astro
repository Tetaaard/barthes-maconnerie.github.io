---
import BaseLayout from "../../layouts/BaseLayout.astro";
const base = import.meta.env.BASE_URL;
---

<BaseLayout title="Enduits terre et chaux — La Paz, Bolivie · Barthès Maçonnerie">
  <article>
    <h2>Enduits terre et chaux — La Paz, Bolivie</h2>

    <p>
      Dans le cadre d’un volontariat, nous avons contribué à la restauration
      d’une tour réalisée exclusivement à partir de matériaux recyclés,
      située sur les hauteurs de La Paz, en Bolivie.
    </p>

    <p>
      Un enduit terre/chaux a été appliqué afin d’assurer une protection durable
      du support, tout en préservant la respiration du bâti et une esthétique
      cohérente avec l’ouvrage existant.
    </p>

    <p>
      Le chantier s’est déroulé dans un contexte technique exigeant, en raison
      de l’altitude et des conditions environnementales particulières du site.
      L’intervention a nécessité une adaptation fine des mises en œuvre
      traditionnelles aux contraintes locales.
    </p>

    <p>
      Ce projet illustre une approche du bâti alliant matériaux écologiques,
      savoir-faire traditionnel et engagement sur un projet d’intérêt
      communautaire.
    </p>

    <!-- Carousel -->
    <div class="carousel">
      <div class="carousel-track">
        <img
          src={`${base}images/realisations/terre-chaux-la-paz/01.jpg`}
          alt="Enduits terre et chaux – chantier à La Paz"
          loading="lazy"
        />
        <img
          src={`${base}images/realisations/terre-chaux-la-paz/02.jpg`}
          alt="Application d’enduit terre/chaux en altitude"
          loading="lazy"
        />
        <img
          src={`${base}images/realisations/terre-chaux-la-paz/03.jpg`}
          alt="Détails d’enduit terre et chaux sur matériaux recyclés"
          loading="lazy"
        />
        <img
          src={`${base}images/realisations/terre-chaux-la-paz/04.jpg`}
          alt="Tour restaurée – enduits terre et chaux"
          loading="lazy"
        />
      </div>

      <button class="carousel-btn prev" aria-label="Image précédente">‹</button>
      <button class="carousel-btn next" aria-label="Image suivante">›</button>
    </div>
  </article>

  <script>
    const carousel = document.querySelector(".carousel");
    if (carousel) {
      const track = carousel.querySelector(".carousel-track");
      const prevBtn = carousel.querySelector(".carousel-btn.prev");
      const nextBtn = carousel.querySelector(".carousel-btn.next");

      const getSlideWidth = () => {
        const img = track.querySelector("img");
        return img ? img.getBoundingClientRect().width + 16 : 0;
      };

      prevBtn.addEventListener("click", () => {
        track.scrollBy({ left: -getSlideWidth(), behavior: "smooth" });
      });

      nextBtn.addEventListener("click", () => {
        track.scrollBy({ left: getSlideWidth(), behavior: "smooth" });
      });
    }
  </script>
</BaseLayout>
