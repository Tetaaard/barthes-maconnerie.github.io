---
import BaseLayout from "../layouts/BaseLayout.astro";
const base = import.meta.env.BASE_URL;
---

<BaseLayout title="Contact — Barthès Maçonnerie">
  <section class="contact-section">
    <div class="contact-wrapper">

      <!-- Image gauche -->
      <div class="contact-image">
        <img
          src={`${base}images/contact.jpg`}
          alt="Atelier de maçonnerie traditionnelle"
        />
      </div>

      <!-- Formulaire droite -->
      <div class="contact-form">
 
    <h2>Un projet, une question, un devis ?</h2>

        <!-- 📍 Zone d’intervention -->
        <p class="contact-location">
          Basés à <strong>Marseille</strong>, déplacements possibles selon les projets
          dans toute la région <strong>Provence</strong>, ainsi que dans
          <strong>l’Hérault</strong> et les <strong>Hauts-Cantons</strong>.
        </p>

        <form
          action="https://formsubmit.co/garance.tartas@gmail.com"
          method="POST"
        >
          <input
            type="hidden"
            name="_subject"
            value="Nouveau message — Barthès Maçonnerie"
          />
          <input type="hidden" name="_template" value="table" />
          <input type="hidden" name="_captcha" value="false" />

          <!-- Redirection après envoi -->
          <input
            type="hidden"
            name="_next"
            value="https://tetaaard.github.io/barthes-maconnerie.github.io/merci/"
          />

          <!-- Anti-spam invisible -->
          <input type="text" name="_honey" style="display:none" />

          <div class="contact-field">
            <label for="name">Nom</label>
            <input
              id="name"
              type="text"
              name="name"
              placeholder="Votre nom"
              required
            />
          </div>

          <div class="contact-field">
            <label for="email">Email</label>
            <input
              id="email"
              type="email"
              name="email"
              placeholder="Votre email"
              required
            />
          </div>

          <div class="contact-field">
            <label for="message">Message</label>
            <textarea
              id="message"
              name="message"
              placeholder="Parlez-nous de votre projet…"
              required
            ></textarea>
          </div>

          <button class="contact-submit" type="submit">
            Envoyer
          </button>
        </form>

        <!-- Instagram moderne -->
        <div class="contact-social">
          <a
            href="https://www.instagram.com/barthes_maconnerie/"
            target="_blank"
            rel="noopener"
            class="instagram-link"
          >
            <!-- Icône Instagram SVG -->
            <svg
              width="20"
              height="20"
              viewBox="0 0 24 24"
              fill="none"
              xmlns="http://www.w3.org/2000/svg"
              aria-hidden="true"
            >
              <rect x="3" y="3" width="18" height="18" rx="5" stroke="currentColor" stroke-width="1.6"/>
              <circle cx="12" cy="12" r="4.2" stroke="currentColor" stroke-width="1.6"/>
              <circle cx="17.5" cy="6.5" r="1" fill="currentColor"/>
            </svg>

            <span>Retrouvez moi sur Instagram</span>
          </a>
        </div>
      </div>
    </div>
  </section>
</BaseLayout>
